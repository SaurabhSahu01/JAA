import React from 'react';
import Registration from '@/components/Registration';

export const metadata = {
  title: 'Registration | JAA',
};

export default function RegistrationPage() {
  return <Registration />;
}
